import { LightningElement, api } from "lwc";

export default class testelead extends LightningElement {
  @api ide;
  @api nome;
  @api empresa;
  @api status;
  @api sobnome;
  @api email;
  @api idade;
  @api classif;  
  
}
